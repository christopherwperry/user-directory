<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>User Directory</title>
  </head>
  <body>
    <h1>User Directory</h1>
    <div class="user-profiles">
      {{#users}}
      <div class="user">
        <img src="{{avatar}}" alt="">
        <a class="name" href="http://localhost:3000/users/{{id}}">{{name}}</a>
        <p class="job">{{job}}, {{company}}</p>
      </div>
      {{/users}}
    </div>

  </body>
</html>
